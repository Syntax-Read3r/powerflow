name: PowerFlow Release

on:
  push:
    tags:
      - 'v*'  # ✅ FIX: Triggers on ANY version tag like v1.0.1, v1.0.2, v2.0.0, etc.

env:
  REPO_NAME: powerflow
  PROFILE_FILE: Microsoft.PowerShell_profile.ps1

jobs:
  validate:
    name: 🔍 Validate Release
    runs-on: windows-latest
    outputs:
      version: ${{ steps.get_version.outputs.version }}
      tag_name: ${{ steps.get_version.outputs.tag_name }}
    
    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4
        
      - name: 🏷️ Get Version from Tag
        id: get_version
        run: |
          $tagName = "${{ github.ref_name }}"
          $version = $tagName -replace '^v', ''
          Write-Output "version=$version" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
          Write-Output "tag_name=$tagName" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
          Write-Host "🏷️ Tag: $tagName"
          Write-Host "📦 Version: $version"
        shell: pwsh
        
      - name: ✅ Validate Profile File Exists
        run: |
          if (-not (Test-Path "${{ env.PROFILE_FILE }}")) {
            Write-Error "❌ Profile file not found: ${{ env.PROFILE_FILE }}"
            exit 1
          }
          Write-Host "✅ Profile file found: ${{ env.PROFILE_FILE }}"
        shell: pwsh
        
      - name: 🔍 Validate Version in Profile
        run: |
          $profileContent = Get-Content "${{ env.PROFILE_FILE }}" -Raw
          $expectedVersion = "${{ steps.get_version.outputs.version }}"
          
          # Check if version is defined in profile
          if ($profileContent -notmatch '\$script:POWERFLOW_VERSION = "([^"]+)"') {
            Write-Error "❌ POWERFLOW_VERSION not found in profile"
            exit 1
          }
          
          $profileVersion = $matches[1]
          Write-Host "📦 Profile version: $profileVersion"
          Write-Host "🏷️ Tag version: $expectedVersion"
          
          if ($profileVersion -ne $expectedVersion) {
            Write-Error "❌ Version mismatch! Profile: $profileVersion, Tag: $expectedVersion"
            Write-Host "💡 Update POWERFLOW_VERSION in profile to match tag"
            exit 1
          }
          
          Write-Host "✅ Version validation passed"
        shell: pwsh
        
      - name: 🧪 Test Profile Syntax
        run: |
          try {
            # Test PowerShell syntax without executing
            $null = [System.Management.Automation.PSParser]::Tokenize((Get-Content "${{ env.PROFILE_FILE }}" -Raw), [ref]$null)
            Write-Host "✅ Profile syntax validation passed"
          } catch {
            Write-Error "❌ Profile syntax error: $($_.Exception.Message)"
            exit 1
          }
        shell: pwsh

  create-release-files:
    name: 📦 Create Release Files
    runs-on: windows-latest
    needs: validate
    
    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4
        
      - name: 📝 Generate Install Script
        run: |
          $version = "${{ needs.validate.outputs.version }}"
          $tagName = "${{ needs.validate.outputs.tag_name }}"
          
          $installScript = @"
          #Requires -Version 5.1
          
          <#
          .SYNOPSIS
              PowerFlow Installation Script v$version
          .DESCRIPTION
              Installs PowerFlow PowerShell profile with all dependencies
          .PARAMETER Force
              Overwrite existing profile without confirmation
          .EXAMPLE
              .\install.ps1
              .\install.ps1 -Force
          #>
          
          param([switch]`$Force)
          
          `$ErrorActionPreference = "Stop"
          
          Write-Host "🚀 PowerFlow v$version Installation" -ForegroundColor Cyan
          Write-Host "=================================================" -ForegroundColor Cyan
          
          # Check PowerShell version
          if (`$PSVersionTable.PSVersion.Major -lt 5) {
              Write-Host "❌ PowerShell 5.1 or higher required" -ForegroundColor Red
              Write-Host "💡 Download PowerShell 7: https://aka.ms/powershell" -ForegroundColor Yellow
              exit 1
          }
          
          # Get profile path
          `$profilePath = `$PROFILE
          `$profileDir = Split-Path `$profilePath -Parent
          
          Write-Host "📁 Profile location: `$profilePath" -ForegroundColor White
          
          # Check if profile exists
          if ((Test-Path `$profilePath) -and -not `$Force) {
              Write-Host "⚠️  PowerShell profile already exists!" -ForegroundColor Yellow
              `$choice = Read-Host "Overwrite existing profile? (y/n)"
              if (`$choice -ne 'y') {
                  Write-Host "❌ Installation cancelled" -ForegroundColor Red
                  exit 1
              }
              
              # Create backup
              `$backupPath = "`$profilePath.backup.`$(Get-Date -Format 'yyyyMMdd-HHmmss')"
              Copy-Item `$profilePath `$backupPath
              Write-Host "💾 Backup created: `$backupPath" -ForegroundColor Cyan
          }
          
          # Create profile directory if needed
          if (-not (Test-Path `$profileDir)) {
              Write-Host "📂 Creating profile directory..." -ForegroundColor Yellow
              New-Item -ItemType Directory -Path `$profileDir -Force | Out-Null
          }
          
          # Download latest profile
          try {
              Write-Host "⬇️  Downloading PowerFlow profile..." -ForegroundColor Yellow
              `$downloadUrl = "https://github.com/Syntax-Read3r/powerflow/releases/download/$tagName/Microsoft.PowerShell_profile.ps1"
              
              # Try release asset first, fallback to raw GitHub
              try {
                  Invoke-RestMethod -Uri `$downloadUrl -OutFile `$profilePath -ErrorAction Stop
              } catch {
                  Write-Host "   📡 Trying alternative download..." -ForegroundColor DarkGray
                  `$fallbackUrl = "https://raw.githubusercontent.com/Syntax-Read3r/powerflow/$tagName/Microsoft.PowerShell_profile.ps1"
                  Invoke-RestMethod -Uri `$fallbackUrl -OutFile `$profilePath -ErrorAction Stop
              }
              
              Write-Host "✅ Profile downloaded successfully" -ForegroundColor Green
          } catch {
              Write-Host "❌ Failed to download profile: `$(`$_.Exception.Message)" -ForegroundColor Red
              Write-Host "💡 Manual download: https://github.com/Syntax-Read3r/powerflow/releases" -ForegroundColor Yellow
              exit 1
          }
          
          Write-Host ""
          Write-Host "🎉 PowerFlow v$version installed successfully!" -ForegroundColor Green
          Write-Host "🔄 Restart PowerShell or run: . ```$PROFILE" -ForegroundColor Cyan
          Write-Host "💡 Type 'pwsh-h' for help after restart" -ForegroundColor Yellow
          Write-Host "🌟 Repository: https://github.com/Syntax-Read3r/powerflow" -ForegroundColor DarkGray
          "@
          
          Set-Content "install.ps1" $installScript -Encoding UTF8
          Write-Host "✅ Generated install.ps1"
        shell: pwsh
        
      - name: 📝 Generate Uninstall Script
        run: |
          $version = "${{ needs.validate.outputs.version }}"
          
          $uninstallScript = @"
          <#
          .SYNOPSIS
              PowerFlow Uninstallation Script v$version
          .DESCRIPTION
              Removes PowerFlow profile and optionally cleans up dependencies
          #>
          
          Write-Host "🗑️  PowerFlow v$version Uninstall" -ForegroundColor Yellow
          Write-Host "=============================================" -ForegroundColor Yellow
          
          `$profilePath = `$PROFILE
          
          if (-not (Test-Path `$profilePath)) {
              Write-Host "ℹ️  No PowerShell profile found at: `$profilePath" -ForegroundColor Yellow
              exit 0
          }
          
          # Check if it's PowerFlow profile
          `$content = Get-Content `$profilePath -Raw -ErrorAction SilentlyContinue
          if (`$content -and `$content -notmatch "PowerFlow") {
              Write-Host "ℹ️  Profile doesn't appear to be PowerFlow" -ForegroundColor Yellow
              `$continue = Read-Host "Remove anyway? (y/n)"
              if (`$continue -ne 'y') {
                  Write-Host "❌ Uninstall cancelled" -ForegroundColor Yellow
                  exit 0
              }
          }
          
          # Backup before removal
          `$backupPath = "`$profilePath.backup.`$(Get-Date -Format 'yyyyMMdd-HHmmss')"
          Write-Host "💾 Creating backup: `$backupPath" -ForegroundColor Cyan
          Copy-Item `$profilePath `$backupPath -ErrorAction SilentlyContinue
          
          # Remove profile
          Remove-Item `$profilePath -Force
          Write-Host "✅ PowerFlow profile removed" -ForegroundColor Green
          Write-Host "💾 Backup saved to: `$backupPath" -ForegroundColor Cyan
          
          # Ask about dependencies
          Write-Host "`n🔧 Remove installed dependencies?" -ForegroundColor Yellow
          Write-Host "   • Starship prompt" -ForegroundColor DarkGray
          Write-Host "   • fzf, zoxide, lsd" -ForegroundColor DarkGray
          Write-Host "   • FiraCode Nerd Font" -ForegroundColor DarkGray
          
          `$removeDeps = Read-Host "Remove dependencies via Scoop? (y/n)"
          if (`$removeDeps -eq 'y') {
              if (Get-Command scoop -ErrorAction SilentlyContinue) {
                  Write-Host "🧹 Removing Scoop packages..." -ForegroundColor Yellow
                  scoop uninstall starship fzf zoxide lsd FiraCode-NF 2>`$null
                  Write-Host "✅ Dependencies removed" -ForegroundColor Green
              } else {
                  Write-Host "⚠️  Scoop not found - manual dependency removal required" -ForegroundColor Yellow
              }
          }
          
          Write-Host "`n✅ PowerFlow uninstall complete" -ForegroundColor Green
          Write-Host "🙏 Thanks for trying PowerFlow!" -ForegroundColor Cyan
          "@
          
          Set-Content "uninstall.ps1" $uninstallScript -Encoding UTF8
          Write-Host "✅ Generated uninstall.ps1"
        shell: pwsh
        
      - name: 📝 Generate Release Notes
        id: release_notes
        run: |
          $tagName = "${{ needs.validate.outputs.tag_name }}"
          $version = "${{ needs.validate.outputs.version }}"
          
          # Try to extract release notes from CHANGELOG.md
          $releaseNotes = ""
          if (Test-Path "CHANGELOG.md") {
              $changelog = Get-Content "CHANGELOG.md" -Raw
              
              # Extract content between current version and next version/unreleased
              $pattern = "(?s)## \[$version\].*?(?=## \[|\z)"
              if ($changelog -match $pattern) {
                  $releaseNotes = $matches[0] -replace "## \[$version\][^\r\n]*[\r\n]*", ""
                  $releaseNotes = $releaseNotes.Trim()
              }
          }
          
          # Fallback release notes
          if (-not $releaseNotes) {
              $releaseNotes = @"
          ## PowerFlow $version
          
          🚀 Enhanced PowerShell profile with beautiful interfaces and productivity tools.
          
          ### 🎯 Key Features
          - Smart navigation with bookmarks and fuzzy search
          - Enhanced Git workflows with beautiful interfaces
          - Auto-installation of dependencies (Starship, fzf, zoxide, lsd)
          - FiraCode Nerd Font integration
          - GitHub repository integration
          - Comprehensive help system and auto-updates
          
          ### 📦 Installation
          ``````powershell
          # Quick install
          irm https://github.com/Syntax-Read3r/powerflow/releases/download/$tagName/install.ps1 | iex
          
          # Or download and run
          Invoke-RestMethod -Uri "https://github.com/Syntax-Read3r/powerflow/releases/download/$tagName/install.ps1" -OutFile "install.ps1"
          .\install.ps1
          ``````
          
          ### 📚 Documentation
          - [Installation Guide](https://github.com/Syntax-Read3r/powerflow/blob/main/docs/installation.md)
          - [Troubleshooting](https://github.com/Syntax-Read3r/powerflow/blob/main/docs/troubleshooting.md)
          - [Features Guide](https://github.com/Syntax-Read3r/powerflow/blob/main/docs/features.md)
          
          ---
          
          **🎉 New to PowerFlow?** Type ``pwsh-h`` after installation for help!
          "@
          }
          
          # Save release notes to file
          Set-Content "RELEASE_NOTES.md" $releaseNotes -Encoding UTF8
          Write-Host "✅ Generated release notes"
        shell: pwsh
        
      - name: 📤 Upload Artifacts
        uses: actions/upload-artifact@v4  # ✅ FIX: Updated to v4
        with:
          name: release-files
          path: |
            ${{ env.PROFILE_FILE }}
            install.ps1
            uninstall.ps1
            RELEASE_NOTES.md

  create-github-release:
    name: 🚀 Create GitHub Release
    runs-on: ubuntu-latest
    needs: [validate, create-release-files]
    permissions:
      contents: write
    
    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4
        
      - name: 📥 Download Release Files
        uses: actions/download-artifact@v4  # ✅ FIX: Updated to v4
        with:
          name: release-files
          path: ./release-files
          
      # ✅ FIX: Modern release action instead of deprecated ones
      - name: 🚀 Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ needs.validate.outputs.tag_name }}
          name: "PowerFlow ${{ needs.validate.outputs.tag_name }}"
          body_path: ./release-files/RELEASE_NOTES.md
          draft: false
          prerelease: false
          files: |
            ./release-files/${{ env.PROFILE_FILE }}
            ./release-files/install.ps1
            ./release-files/uninstall.ps1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  post-release:
    name: 📢 Post-Release Tasks
    runs-on: ubuntu-latest
    needs: [validate, create-github-release]
    if: success()
    
    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4
        
      - name: 🎉 Release Success Notification
        run: |
          echo "🎉 PowerFlow ${{ needs.validate.outputs.tag_name }} released successfully!"
          echo "📦 Release URL: https://github.com/Syntax-Read3r/powerflow/releases/tag/${{ needs.validate.outputs.tag_name }}"
          echo "📥 Install command: irm https://github.com/Syntax-Read3r/powerflow/releases/download/${{ needs.validate.outputs.tag_name }}/install.ps1 | iex"